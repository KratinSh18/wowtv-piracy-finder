# WOW TV Piracy Finder - title search + website.
# Install everything with:  pip install -r requirements.txt   (Windows)
#                           pip3 install -r requirements.txt  (Mac / Linux)

ddgs>=9.0               # keyless whole-web search (DuckDuckGo)
rapidfuzz>=3.0          # fast, Unicode-safe fuzzy title matching (typo-tolerant)
deep-translator>=1.11   # regional-language title translation (Tamil/Telugu/...)
openpyxl>=3.1           # read the .xlsx tracker in extract_catalog.py
yt-dlp                  # search YouTube for re-uploads (works on Mac/Windows/Linux)

# Optional - only for the content-fingerprint feature (the `scan` / `selftest`
# commands). NOT needed for the title finder or the website. Install if used:
#   pip install numpy scipy Pillow
#   (plus ffmpeg on PATH for real media)
